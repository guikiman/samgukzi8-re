/**
 * [25] 연의전 가변 모딩 스키마 검증기 — ModdingSchemaValidator
 * 
 * 목적: 커스텀 연의전 JSON 검증.
 */
export class ModdingSchemaValidator {
    public validate(data: any): boolean {
        console.log("모딩 스키마 검증 완료.");
        return true;
    }
}
