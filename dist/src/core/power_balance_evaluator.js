/**
 * [52] 세력 균형 평가 모듈 — PowerBalanceEvaluator
 *
 * 목적: 인접 세력과의 힘의 균형 평가.
 */
export class PowerBalanceEvaluator {
    evaluate(myPower, enemyPower) {
        return myPower > enemyPower ? 'STRONG' : 'WEAK';
    }
}
//# sourceMappingURL=power_balance_evaluator.js.map