/**
 * [92] 라이벌 경쟁 관계 엔진 — RivalryEngine
 *
 * 목적: 장수 간 경쟁 유도.
 */
export class RivalryEngine {
    triggerRivalry(a, b) {
        console.log(`[Social] ${a.name}와 ${b.name}의 경쟁 관계 활성화.`);
    }
}
//# sourceMappingURL=rivalry_engine.js.map