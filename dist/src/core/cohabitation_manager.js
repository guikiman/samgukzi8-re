/**
 * [93] 동거 및 가신 영입 — CoHabitationManager
 *
 * 목적: 동거 및 가신 관계 처리.
 */
export class CoHabitationManager {
    setupCoHabitation(officer, host) {
        console.log(`[Social] ${officer.name}이 ${host.name}의 가신으로 합류.`);
    }
}
//# sourceMappingURL=cohabitation_manager.js.map