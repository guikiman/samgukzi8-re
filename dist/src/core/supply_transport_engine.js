/**
 * [34] 수송 파이프라인 — SupplyTransportEngine
 *
 * 목적: 도시 간 자원 수송.
 */
export class SupplyTransportEngine {
    transport(fromCity, toCity, amount) {
        console.log(`[Transport] ${fromCity} -> ${toCity} 수송 중.`);
    }
}
//# sourceMappingURL=supply_transport_engine.js.map