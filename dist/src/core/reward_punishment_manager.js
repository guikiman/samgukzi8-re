/**
 * [68] 상벌 집행 모듈 — RewardPunishmentManager
 *
 * 목적: 공적에 따른 상벌 처리.
 */
export class RewardPunishmentManager {
    applyReward(officer, rewardType) {
        console.log(`[Reward] ${officer.name}에게 ${rewardType} 수여.`);
    }
}
//# sourceMappingURL=reward_punishment_manager.js.map